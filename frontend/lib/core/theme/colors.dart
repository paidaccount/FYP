import 'package:flutter/material.dart';

class AppColors {
  // 🔹 Modern Cyber-Fleet & Control Center Canvas
  static const Color background = Color(0xFFF8FAFC);       // Ultra-clean Cool Slate Canvas (Slate-50)
  static const Color surface = Color(0xFFFFFFFF);          // Crisp Pure White Card Surface
  static const Color cardColor = Color(0xFFFFFFFF);
  static const Color cardSurface = Color(0xFFFFFFFF);
  static const Color secondarySurface = Color(0xFFF1F5F9); // Subtle Pill & Metric Background (Slate-100)

  // 🔹 High-Contrast Modern Typography & Hairline Borders
  static const Color textPrimary = Color(0xFF0F172A);      // Deep Obsidian Slate (Slate-900)
  static const Color textSecondary = Color(0xFF64748B);    // Refined Muted Slate (Slate-500)
  static const Color textTertiary = Color(0xFF94A3B8);     // Subtle Meta Label (Slate-400)
  static const Color border = Color(0xFFE2E8F0);           // Hairline Border (Slate-200)
  static const Color borderStrong = Color(0xFFCBD5E1);     // Active Border (Slate-300)

  // 🔹 Primary Brand & Semantic Accents
  static const Color primary = Color(0xFFE65F2B);          // Signature Automotive Safety Orange
  static const Color primaryLight = Color(0xFFFFF0EB);     // Soft Safety Tint
  static const Color primaryVariant = Color(0xFFCC4E1D);   // Deep Safety Orange
  static const Color secondary = Color(0xFF0F172A);        // Deep Slate Structure
  static const Color secondaryVariant = Color(0xFF1E293B);

  // 🔹 Advanced High-Tech Domain Colors
  static const Color safe = Color(0xFF10B981);             // Verified Node / Connected / Safe (Emerald)
  static const Color safeLight = Color(0xFFECFDF5);        // Subtle Safe Pill Tint
  static const Color accent = Color(0xFF06B6D4);           // V2X Radar / Telemetry Cyan
  static const Color accentLight = Color(0xFFECFEFF);
  static const Color aiHighlight = Color(0xFF6366F1);       // XAI / Machine Learning Indigo
  static const Color aiHighlightLight = Color(0xFFEEF2FF);
  static const Color warning = Color(0xFFF59E0B);          // Suspicious Telemetry / Warning (Amber)
  static const Color warningLight = Color(0xFFFFFBEB);
  static const Color error = Color(0xFFEF4444);            // Malicious Attack / Siren Red (Rose)
  static const Color errorLight = Color(0xFFFEF2F2);

  // 🔹 Material Compatibility Tokens
  static const Color onPrimary = Colors.white;
  static const Color onSecondary = Colors.white;
  static const Color onBackground = Color(0xFF0F172A);
  static const Color onSurface = Color(0xFF0F172A);
  static const Color onError = Colors.white;
}
